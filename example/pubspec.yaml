name: example
description: "Demonstrates how to use the equation_painter package."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  equation_painter:
    path: ../
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  fl_chart: ^1.2.0
  intl: ^0.20.3

flutter:
  uses-material-design: true
